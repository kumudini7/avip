from typing import Any


def analyze_text(text: str) -> dict[str, Any]:
    return {
        "summary": "AI analysis placeholder for the uploaded process document.",
        "process_steps": ["Identify steps", "Classify work", "Score complexity"],
        "input_length": len(text),
    }

